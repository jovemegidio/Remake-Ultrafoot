# pnpm 11: não bloquear build/dev no gate de aprovação de scripts de dependências.
# sharp não é usado (images.unoptimized = true) e msw é só mock — seus build
# scripts podem ficar sem rodar sem afetar o launcher.
verify-deps-before-run=false
strict-dep-builds=false
